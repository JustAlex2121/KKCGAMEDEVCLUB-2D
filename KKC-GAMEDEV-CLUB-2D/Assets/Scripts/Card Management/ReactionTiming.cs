using UnityEngine;

public enum ReactionTiming
// This enum represents the timing of a reaction in relation to a game action. It can be either PRE (before the action is performed) or POST (after the action is performed).
{
    PRE,
    POST
}
